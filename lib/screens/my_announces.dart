import 'package:flutter/material.dart';

class MyAnnonces extends StatelessWidget {
  const MyAnnonces({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
